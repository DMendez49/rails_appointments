<h1>Add Hospital</h1>
  <%= form_with(model: @hospital, local: true, class: "ui form" ) do |f| %>
    <div class="field">
      <%= f.text_field :name, autofocus: true, placeholder: "Hospital Name", required: true %>
    </div>
    <%= f.submit class: "ui green button" %>
  <% end %>